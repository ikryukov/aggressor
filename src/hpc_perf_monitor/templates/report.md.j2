# Performance Analysis Report

Generated on {{ timestamp }}

{% if commit_info %}
## Commit Information
{% for key, value in commit_info.items() %}
- **{{ key }}**: {{ value }}
{% endfor %}
{% endif %}

## Summary Statistics

### Latency Differences
- Average: {{ "%.2f"|format(summary.avg_latency_diff) }}%
- Maximum: {{ "%.2f"|format(summary.max_latency_diff) }}%
- Minimum: {{ "%.2f"|format(summary.min_latency_diff) }}%

### Bandwidth Differences
- Average: {{ "%.2f"|format(summary.avg_bandwidth_diff) }}%
- Maximum: {{ "%.2f"|format(summary.max_bandwidth_diff) }}%
- Minimum: {{ "%.2f"|format(summary.min_bandwidth_diff) }}%

## Detailed Results

{{ table }}

{% if significant_changes %}
## Significant Changes

| Message Size | Latency Difference (%) | Bandwidth Difference (%) |
|--------------|------------------------|--------------------------|
{% for change in significant_changes %}
| {{ change.msg_size }} | {{ "%.2f"|format(change.latency_diff_pct) }} | {{ "%.2f"|format(change.bandwidth_diff_pct) }} |
{% endfor %}
{% endif %} 